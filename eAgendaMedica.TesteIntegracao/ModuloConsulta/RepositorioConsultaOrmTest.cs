using eAgendaMedica.Dominio.Compartilhado;
using eAgendaMedica.Dominio.ModuloConsulta;
using eAgendaMedica.TestesIntegracao.Compartilhado;
using FizzWare.NBuilder;
using FluentAssertions;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace LocadoraDeVeiculos.TesteIntegracao.ModuloCliente
{
    [TestClass]
    public class RepositorioClienteEmOrmTest : TestesIntegracaoBase
    {
        //[TestMethod]
        //public async Task Deve_Inserir_Consulta()
        //{
        //    var consulta = Builder<Consulta>.CreateNew().Build();

        //    await repositorioConsulta.InserirAsync(consulta);

        //    await contextoPersistencia.GravarAsync();

        //    var consultaSelecionada = await repositorioConsulta.SelecionarPorIdAsync(consulta.Id);

        //    consultaSelecionada.Should().BeEquivalentTo(consulta);
        //}

    }
}
